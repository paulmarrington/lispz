### Libraries >> jQuery
New Wave JavaScript

https://github.com/jquery/jquery
###
(using [net cdnjs]
  (ref build (lambda [target-repo]
    (return (cdnjs.build target-repo "jquery" [[
      {repo: "jquery" files: [[
        {exclude: '/\.map$|\.min.js$/'}
      ]]}
    ]]))
  ))
  (net.loader "jquery" window.jQuery { build })
)
