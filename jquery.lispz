(using [net cdnjs]
  (var build (lambda [target-repo]
    (return (cdnjs.build target-repo "jquery" [[
      {repo: "jquery" files: [[
        {exclude: '/\.map$|\.min.js$/'}
      ]]}
    ]]))
  ))
  (var loaded (net.script "ext/jquery.js" (=> (return window.jQuery))))
  (when  loaded [] (export {build}))
  (catch loaded [] (export {build}))
)
